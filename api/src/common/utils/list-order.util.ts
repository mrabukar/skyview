/** List default: most recently changed first (updatedAt when present, else createdAt). */
export const orderByUpdatedAtDesc = { updatedAt: "desc" as const };
export const orderByCreatedAtDesc = { createdAt: "desc" as const };
